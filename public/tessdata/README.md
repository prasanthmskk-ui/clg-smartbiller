# Place the English OCR language pack here:  public/tessdata/eng.traineddata.gz
#
# Download it once (requires network, then never again):
#   npm run fetch-tessdata
#
# The service worker (public/sw.js) caches this file on first load so OCR keeps
# working with zero network requests afterwards.
